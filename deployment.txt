===============================================================================
 PULSEROOM - DEPLOYMENT SOLUTION (FREE, NO PLATFORM COST)
 Date: 14-Aug-2026
 Updated: 16-Aug-2026 - >>> DEPLOYED & LIVE <<<
   Render service  : pulseroom  ->  https://pulseroom-jtaj.onrender.com
   Supabase project: PulseRoom  ->  db.nnqudmojouzodgbemukp.supabase.co
   Status          : Connected to PostgreSQL (session pooler) - durable.
   Verified in log : "Connected to PostgreSQL database successfully."
===============================================================================
MY RECOMMENDATION: ONE Render Web Service running the FULL Node app
  + Supabase (free) Postgres for durable data.
  Everything works: real-time chat, uploads, push, background jobs.
  Only ONE tiny code change is required (3 lines) - everything else is
  environment variables in the Render dashboard.

------------------------------------------------------------------------------
 WHY THIS (and not the other options)
------------------------------------------------------------------------------
  - "Frontend on Render + Supabase" alone will NOT work: the app's real-time
    chat (Socket.IO) and all /api/* calls come from the Node server. Without
    it nothing works. (Supabase stores data; it is not the chat backend.)
  - "Data on your own PC" works only if your PC is online 24/7, has a public
    IP / DDNS + port forwarding, and you accept exposing PostgreSQL to the
    internet. Fine for personal testing, bad for real users.
  - One Render service hosting the WHOLE app avoids all cross-origin, CORS,
    and socket-URL problems because the frontend and backend share one domain.

------------------------------------------------------------------------------
 WHAT GETS DEPLOYED WHERE
------------------------------------------------------------------------------
  Render free Web Service  -> the whole app (server/ + built client/dist)
  Supabase free Postgres   -> durable database (accounts survive redeploys)
  GitHub                   -> your code repo (Render auto-redeploys on push)

------------------------------------------------------------------------------
 THE ONLY CODE CHANGE NEEDED (3 lines) - serve the built frontend  >>> DONE <<<
------------------------------------------------------------------------------
  APPLIED on 16-Aug-2026 (server/src/server.js, just before the initDb().then
  boot block): the Node server now serves the built React app. It is
  cwd-robust - it finds client/dist under either <repo>/client/dist or
  <repo-root-parent>/client/dist depending on where the platform runs
  `node src/server.js`:

    const clientDist = [
      path.resolve(process.cwd(), 'client', 'dist'),
      path.resolve(process.cwd(), '..', 'client', 'dist')
    ].find((p) => fs.existsSync(p));
    if (clientDist) {
      app.use(express.static(clientDist));
      app.get(/^\/(?!api\/|uploads\/|socket\.io).*/, (req, res, next) => {
        if (req.path.includes('.')) return next();
        res.sendFile(path.join(clientDist, 'index.html'));
      });
    }

  Verified: server test suite still passes (14/14). No further code changes
  are needed. Everything below is just settings.

  IMPORTANT (16-Aug-2026): Render applies env vars during the build too, so
  NODE_ENV=production makes `npm install` skip dev-dependencies and the client
  build fails with "vite: not found". The client install MUST use
  `npm install --include=dev`:

      Build Command:
      cd server && npm install && cd ../client && npm install --include=dev && npm run build

  (If you refuse any code change: deploy client/dist as a Render Static Site
   and the server as a second Web Service - but then the client must also be
   told the backend URL, which is MORE changes, and real-time needs a socket
   URL edit. The single-service route above is the least-change path.)

------------------------------------------------------------------------------
 1. SET UP THE FREE DATABASE (Supabase) - solves the data-loss problem
------------------------------------------------------------------------------
  Your real problem: data/ on Render's free disk is wiped on every redeploy,
  so users would lose accounts. Fix = put the DB in the cloud (free, durable):

  1. Sign up at supabase.com (free plan, no card).
  2. Create a project -> note these:
       - Database password you set
       - Settings -> Database -> "Connection string" (postgresql://...)
  3. That Postgres is permanent and free. On the NEXT Render deploy nothing is
     lost because all data lives in Supabase, not on the server disk.

  The server already auto-creates + migrates every table on first connect
  (it runs in "PostgreSQL mode" whenever DATABASE_URL is reachable).

------------------------------------------------------------------------------
 2. GITHUB
------------------------------------------------------------------------------
  1. Push this project to a GitHub repo (make sure node_modules / dist /
     uploads / data / .env stay gitignored - they already are).
  2. Render reads from GitHub, so every git push auto-redeploys.

------------------------------------------------------------------------------
 3. RENDER - CREATE THE WEB SERVICE (free)
------------------------------------------------------------------------------
  1. render.com -> New -> Web Service -> connect your GitHub repo.
  2. Service settings:
       Name:            pulseroom
       Region:          any (nearest to your users)
       Build Command:   cd server && npm install && cd ../client && npm install --include=dev && npm run build
                        (--include=dev is REQUIRED: with NODE_ENV=production Render
                         skips dev-dependencies during the build and the client
                         build then fails with "vite: not found")
       Start Command:   cd server && node src/server.js
                        (Render runs commands from the REPO ROOT, so the server's
                         src/ must be entered via `cd server` first)
       Instance Type:   Free
   3. Environment Variables (Project/Settings -> Environment) - THE CRITICAL PART:
       NODE_ENV              = production          (turns on PG SSL - required by Supabase)
       DATABASE_URL          = postgresql://...    (Supabase SESSION POOLER string - see below)
       JWT_SECRET            = <a long random string>  (REQUIRED - server refuses to
                                                        start without it in prod)
       GMAIL_USER            = your@gmail.com      (for email confirmations)
       GMAIL_APP_PASS        = <16-char Gmail app password>  (free, enable 2FA -> app pass)
       CLIENT_URL            = https://pulseroom-jtaj.onrender.com
       APP_URL               = https://pulseroom-jtaj.onrender.com
       VAPID_PUBLIC_KEY      = <paste the public key from data/.vapid.json>
       VAPID_PRIVATE_KEY     = <paste the private key from data/.vapid.json>
       PUSH_CONTACT_EMAIL    = admin@example.com
     NOTE: JWT_SECRET and the VAPID keys MUST be env vars (not files), because
     data/ is wiped on redeploys. Without fixed VAPID keys, every redeploy
     invalidates all users' push subscriptions.

  DATABASE_URL - SUPABASE CONNECTION METHOD (learned the hard way):
  - Use the SESSION POOLER string, NOT the "Direct connection":
        postgresql://postgres.nnqudmojouzodgbemukp:PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres
  - Why: Supabase direct connections are IPv6-first. Render's free tier has no
    IPv6 route, so the direct host fails with ENETUNREACH and the server falls
    back to the in-memory DB (data lost on every redeploy). The session pooler
    is IPv4 and works. The "Transaction pooler" (port 6543) also works if the
    session pooler is ever unavailable.
  - Password tips: avoid special characters in the DB password entirely
    (@#$% break the URL - '@' must be encoded as %40). If you change the
    password, update DATABASE_URL too.
  - Code fix already in place (server/src/db.js): dns.setDefaultResultOrder
    ('ipv4first') + family:4 so IPv4-capable hosts are used where available.

------------------------------------------------------------------------------
 4. AFTER DEPLOY - CHECKLIST
------------------------------------------------------------------------------
  - Open https://pulseroom-jtaj.onrender.com -> register -> confirm email.
  - Register a second account (2nd browser/incognito) -> message each other
    -> messages appear live on BOTH sides (proves Socket.IO works).
  - Push a small git change -> wait for redeploy -> log in again with the SAME
    account. It still exists = data persistence confirmed.
  - Images/voice notes upload (proves multer upload path works).

------------------------------------------------------------------------------
 5. FREE-TIER LIMITS TO EXPECT (not blockers)
------------------------------------------------------------------------------
  - Render free sleeps after ~15 min of inactivity. First load after sleep
    takes 30-60s to wake (Socket.IO reconnects automatically). Everyone sees
    this on free Render; it's normal.
  - Render free = 512 MB RAM + ephemeral disk. Not a problem since the DB is
    in Supabase.
  - Uploads over ~4.5 MB may be limited by platform; fine for normal use.
  - Supabase free is durable and does NOT wipe on deploy - that was the old
    data/ problem and it is now gone.

------------------------------------------------------------------------------
 WHY THIS SOLVES "USERS LOSE ACCOUNTS ON EVERY DEPLOY"
------------------------------------------------------------------------------
  Accounts/rooms/messages are written to Supabase Postgres, which lives
  outside Render. Redeploys only replace the code + temporary files on the
  Render disk; the database is untouched. Logins, chats, and push all survive
  every git push.

------------------------------------------------------------------------------
 FULL FEATURES AVAILABLE ON THIS SETUP (everything works)
------------------------------------------------------------------------------
  Real-time send/receive, typing, presence, read receipts, voice/video calls,
  statuses + expiry job, E2EE, media uploads, star/edit/forward/delete, group
  rooms + bridges, reports, block/privacy, scheduled messages, notifications.
